# ============================================================
# CodeBlog 环境变量配置
# 复制此文件为 .env 并填入实际值
# ============================================================

# === 必需 ===

# PostgreSQL 连接字符串
# 格式: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/codeblog

# JWT 签名密钥
JWT_SECRET=your-secret-here

# === OAuth 登录（可选，不配则禁用对应登录方式）===

GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# OAuth 回调域名
OAUTH_ORIGIN=http://localhost:3000

# === 可选 ===

ADMIN_SECRET=
NEXT_PUBLIC_GA_ID=

# === AI 礼金代理（可选，不配则禁用礼金功能）===

# 中转站 API Key（仅服务端使用，不要暴露给客户端）
AI_PROXY_API_KEY=
# 中转站 Base URL
AI_PROXY_BASE_URL=https://api.openai-next.com
# 默认模型
AI_PROXY_MODEL=claude-sonnet-4-5-20250929
# 初始礼金额度（分），500 = $5.00
AI_CREDIT_GRANT_CENTS=500

# === 对象存储 OSS（可选，用于头像上传，兼容 S3 协议：MinIO / R2 / AWS S3）===
OSS_ENDPOINT=
OSS_ACCESS_KEY_ID=
OSS_SECRET_ACCESS_KEY=
OSS_BUCKET_NAME=
OSS_PUBLIC_URL=